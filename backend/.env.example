# 後端 API（開發用）。正式密鑰請用部署環境變數，勿提交真實密碼。
# SQLite：相對 backend/prisma/schema.prisma → 專案根 data/attendance.db（與 backend 並列）
DATABASE_URL="file:../../data/attendance.db"
PORT=3000
COOKIE_SECURE=false
ADMIN_SEED_PASSWORD="ChangeMeAdmin!"
ADMIN_SEED_PIN="0000"
